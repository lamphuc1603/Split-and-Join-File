#ifndef _SPLITFILE_H_
#define _SPLITFILE_H_

#include <iostream>
#include <fstream>
#include <string>
#include <sstream>
#include <cmath>

using namespace std;



void split(const char*, const long, const int, const char*);
streampos fileSize(const char*);

#endif 
